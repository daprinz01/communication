apiVersion: v1
kind: Service
metadata:
  name: communication-service
  namespace: default
spec:
  ports:
  - port: 8083
    protocol: TCP
    targetPort: 8083
  selector:
    name: communication
  type: NodePort